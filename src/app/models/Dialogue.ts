export type Dialogue = {
  buttons?: string[]
  // triggerKey: string
  // input?: string
  // buttonSize: 
  maxNameLength?: number 
  form?: {
    inputLabel: string
    inputValue: string
    // buttons: string[]
  }
}